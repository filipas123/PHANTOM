# AI Improvements - UI

## What we have
- Basic chat interface with simple iframe preview.
- Limited interaction with generated tools.

## What we want
- More powerful windows for code demonstration (`show_code_demo`).
- Graph visualizer capabilities that naturally open in new tabs/windows.
- Autonomous and visual UI elements that pop out out of the box.

## What is done
- Added `open_new_window` flag support correctly.
- Enabled auto-popout for `show_code_demo` and `analyze_target_graph`.
