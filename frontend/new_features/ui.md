# AI Sync - UI

## What we have
- HTML preview panel via iframe.
- Image OSINT (drag & drop).
- A basic popout button that uses window.open with raw HTML writing.

## What we want
- A new web-ui capability to show code, demos, and graphs in a new window/panel with proper HTML framing for more freedom.
- Provide the AI with an autonomous capability to trigger popouts immediately based on a boolean flag.

## What is done
- Analyzed existing `show_preview_window`.
