{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "properties": {
    "config_file": {
      "type": "string",
      "description": "Path to the configuration file to audit."
    },
    "benchmark": {
      "type": "string",
      "description": "Security benchmark to check against (e.g., CIS, NIST)."
    }
  },
  "required": ["config_file"]
}
